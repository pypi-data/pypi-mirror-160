from setuptools import setup

setup(name='jmtor_probdistributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['jmtor_probdistributions'],
      zip_safe=False)
