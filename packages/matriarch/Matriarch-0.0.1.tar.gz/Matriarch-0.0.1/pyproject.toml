[tool.poetry]
name = "Matriarch"
version = "0.0.1"
description = "Matriarch"
authors = ["Quansight"]
readme = "README.md"
repository = "https://github.com/quansight"

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]
pytest = "*"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
