[tool.poetry]
name = "sqlsl"
version = "0.1.0"
description = "SQL simple loader"
authors = ["pegov"]
license = "MIT"
repository = "https://github.com/pegov/sqlsl"
keywords = ["sql"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
flake8 = "^4.0.1"
isort = "^5.10.1"
autoflake = "^1.4"
black = "^22.6.0"
mypy = "^0.971"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
