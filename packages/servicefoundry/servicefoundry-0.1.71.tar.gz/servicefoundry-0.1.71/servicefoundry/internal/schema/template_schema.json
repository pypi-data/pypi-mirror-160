{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://truefoundry.com/v1/template",
  "title": "Template schema for defining template,",
  "type": "object",
  "properties": {
    "kind": {
      "type": "string",
      "description": "For a template schematic, must be Template.",
      "enum": [
        "template"
      ]
    },
    "metadata": {
      "type": "object",
      "description": "Component metadata.",
      "properties": {
        "name": {
          "description": "string"
        },
        "labels": {
          "type": "object",
          "description": "A set of string key/value pairs used as arbitrary labels on this component.",
          "additionalProperties": {
            "type": "string"
          }
        },
        "annotations": {
          "type": "object",
          "description": "A set of string key/value pairs used as arbitrary annotations on this component.",
          "additionalProperties": {
            "type": "string"
          }
        }
      }
    },
    "spec": {
      "$ref": "#/definitions/templateSpec",
      "description": "A container for all remaining attributes."
    }
  },
  "required": [
    "kind",
    "metadata",
    "spec"
  ],
  "additionalProperties": false,
  "definitions": {
    "templateSpec": {
      "type": "object",
      "properties": {
        "parameters": {
          "type": "array",
          "description": "The component's configuration options.",
          "items": {
            "$ref": "#/definitions/templateParameter"
          }
        },
        "baseBuild": {
          "type": "string",
          "description": "Base service definition this template is extending."
        },
        "baseComponent": {
          "type": "string",
          "description": "Base component definition this template is extending."
        },
        "overwrites": {
          "type": "object",
          "description": "Overwrites done on top of base service definitions."
        },
        "comments": {
          "type": "string",
          "description": "Comments which need to be added in generated yaml file."
        },
        "postInitInstruction": {
          "type": "string",
          "description": "Instructions for the user after successful initialization of service."
        }
      },
      "required": [
        "parameters",
        "baseBuild",
        "baseComponent",
        "overwrites"
      ],
      "additionalProperties": false
    },
    "templateParameter": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Base service definition this template is extending."
        },
        "kind": {
          "type": "string",
          "description": "Overwrites done on top of base service definitions."
        },
        "prompt": {
          "type": "string",
          "description": "Base service definition this template is extending."
        },
        "options": {
          "type": "array",
          "description": "Overwrites done on top of base service definitions."
        },
        "suggest": {
          "type": "string",
          "description": "Value suggested for interactive use."
        },
        "default": {
          "type": [
            "string",
            "integer"
          ],
          "description": "Overwrites done on top of base service definitions."
        }
      },
      "required": [
        "id",
        "kind"
      ]
    },
    "additionalProperties": false
  }
}
