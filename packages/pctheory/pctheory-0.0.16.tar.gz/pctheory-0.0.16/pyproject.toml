[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name="pctheory"
version = "0.0.16"
dependencies = [
    "numpy",
    "matplotlib",
    "networkx",
    "pyvis",
]
