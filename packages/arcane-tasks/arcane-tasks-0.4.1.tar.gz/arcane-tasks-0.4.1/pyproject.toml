[tool.poetry]
name = "arcane-tasks"
version = "0.4.1"
description = "Override tasks client"
readme = "README.md"
authors = ["Arcane <product@arcane.run>"]
packages = [
    { include = "arcane" }
]

[tool.poetry.dependencies]
python = "^3.6"
google-cloud-tasks = "2.8.1"
backoff = "1.10.0"
arcane-core = "^1.6.0"
[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
